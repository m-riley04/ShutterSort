#include "Sorter.h"
#include <exiv2/exiv2.hpp>
#include <iostream>

}


        } else {
            std::cerr << "Source directory doesn't exist or is not a directory." << std::endl;
        }
    }
}
